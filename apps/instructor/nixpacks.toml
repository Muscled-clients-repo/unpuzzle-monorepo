# nixpacks.toml
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --loglevel=error"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:test"

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"