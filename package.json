{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo 'use per-app build scripts instead'"
  },
  "devDependencies": {
    "@types/node": "^24.1.0"
  }
}
